# Kensho — AI-powered Study Notes

Kensho generates study notes from any source using AI. Built with Laravel 11, Breeze (Livewire), and Tailwind CSS.

Quick start:

```powershell
if (!(Test-Path "database/database.sqlite")) { ni "database/database.sqlite" -ItemType File | Out-Null }
composer install
npm install
php artisan migrate
php artisan serve
```

Repository: https://github.com/Naseem7888/Kensho-AI-Powered-Study-Notes-from-Any-Source
